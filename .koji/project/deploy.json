{
  "deploy": {
    "frontend": {
      "output": "build",
      "type": "static",
      "commands": [
        "npm install",
        "export NODE_ENV=production && npm run build"
      ]
    }
  }
}
